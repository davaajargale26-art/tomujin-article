{
  "name": "tomujin-article-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend install",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.22.3"
  }
}
